type Props = {};

const Navbar = (props: Props) => {
  return (
    <div className="text-2xl relative z-30  m-3 text-white font-semibold">
      Home/Dashboard
    </div>
  );
};

export default Navbar;
